# configs/model/xgboost.yaml
model_type: "xgboost"
learning_rate: 0.1
max_depth: 6
n_estimators: 100
random_state: 42
scale_pos_weight: 1  # Will be dynamically overwritten if use_smote is false and we calculate it
